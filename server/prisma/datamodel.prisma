type Event {
  id: ID! @unique
  slug: String! @unique
  title: String!
  description: String
  dates: [Date]
  places: [Place]
  menus: [String]
  participants: [Participant]
}

type Place {
  id: ID! @unique
  name: String!
  url: String
  event: Event!
}

type Date {
  id: ID! @unique
  timestamp: String!
  event: Event!
}

type Participant {
  id: ID! @unique
  name: String!
  dates: [String]
  event: Event!
}
