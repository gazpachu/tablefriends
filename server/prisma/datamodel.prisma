type Event {
  id: ID! @unique
  slug: String! @unique
  title: String!
  description: String
  dates: [String]
  places: [Place]
  menus: [String]
}

type Place {
  id: ID! @unique
  name: String!
  url: String
  event: Event!
}
